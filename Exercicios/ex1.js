// Criar uma função que retorna a string "Olá, " concatenada
// com um argumento text (a ser passado para a função) 
// e com ponto de exclamação "!" no final

let text = "Jose";

function cumprimentar(text) {
    console.log("Olá, " + text + "!");
}

cumprimentar(text);